hadoop_home: /opt/hadoop-2.X
mirrors: [ "http://xenia.sote.hu/ftp/mirrors/www.apache.org/hadoop/common/hadoop-2.6.0", "http://ftp.itu.edu.tr/Mirror/Apache/hadoop/common/hadoop-2.6.0" ]
hadoop_master: "{{hostvars['master']['ansible_host']}}"
hadoop_slaves:
  - "{{hostvars['slave01']['ansible_host']}}"
  - "{{hostvars['slave02']['ansible_host']}}"

master_hostname: "{{hostvars['master']['ansible_hostname']}}"
slaves_hostnames:
  - "{{hostvars['slave01']['ansible_hostname']}}"
  - "{{hostvars['slave02']['ansible_hostname']}}"

namenode_dir: "/opt/hdfs/namenode"
datanode_dir: "/opt/hdfs/datanode"
